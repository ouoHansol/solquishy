# Slangi sprite QA

- `idle`: pass — all 4 declared frames extracted cleanly; the sequence reads as settle → compress → rebound → wink, with stable identity and a soft readable loop seam.
- Automated extraction and atlas reports: pass.
- Chroma cleanup: pass; no visible green fringe in the transparent contact sheet.
